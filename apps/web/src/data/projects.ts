// Placeholder for project data functions
// This file would typically contain database queries for projects

export async function getAllReviewedProjects() {
  // Return empty array for now - would typically fetch from database
  return [];
}

export async function getProjectById(id: string) {
  // Return null for now - would typically fetch from database
  return null;
}